<template>
  <el-button class="lq-attachment" type="text" :disabled="!enabled" @click="download">
    <span class="icon fa fa-paperclip"></span>
    <span class="file-name">{{fileName}}</span>
    <el-button class="close-button" v-if="closable" type="text" icon="el-icon-close" @click="remove"></el-button>
  </el-button>
</template>

<script>
export default {

  props: {
    file: {               // the attachment (File topic, plain Object)
      type: Object,
      required: true
    },
    enabled: Boolean,
    closable: Boolean     // if true the close-button is rendered, optional
  },

  computed: {

    fileName () {
      return this.file.children['dmx.files.file_name'].value
    },

    path () {
      return this.file.children['dmx.files.path'].value
    }
  },

  methods: {

    download () {
      this.$store.dispatch('downloadFile', this.path)
    },

    remove () {
      this.$emit('remove', this.file)
    }
  }
}
</script>

<style>
.lq-attachment {
  display: block;                   /* Element UI Button default is "inline-block" */
  margin-left: 0 !important;        /* Element UI Button default is 10px for consecutive buttons  */
  white-space: normal;              /* Break long file names; Element UI Button default is "nowrap" */
  text-align: left;                 /* Element UI Button default is "center" */
  line-height: 1.2;                 /* Element UI Button default is 1 */
  width: 100%;
}

.lq-attachment.is-disabled {
  color: var(--secondary-color);    /* Element UI Button default is #c0c4cc */
  cursor: unset !important;
}

.lq-attachment > span {             /* outer span is generated by Element UI */
  display: flex;
}

.lq-attachment .file-name {
  font-size: var(--secondary-font-size);
}

.lq-attachment .icon {
  flex-basis: 20px;
  flex-shrink: 0;
  font-size: 16px;
}

.lq-attachment .close-button {
  flex-basis: 24px;
  flex-shrink: 0;
  font-size: 16px;
  visibility: hidden;
}

.lq-attachment:hover .close-button {
  visibility: visible;
}
</style>
